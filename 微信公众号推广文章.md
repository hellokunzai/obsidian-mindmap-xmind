# 在 Obsidian 里直接编辑 XMind，这款开源插件把「切换工具」这件事干掉了

> 一个被无数 Obsidian 用户念叨的需求，我们用 15 个版本把它磨了出来。

---

## ▌为什么你需要它

很多用 Obsidian 做知识管理的人，都会撞上同一个尴尬：

笔记写得好好的，想画一张思维导图把逻辑理一理，就得切到 XMind 或别的工具；画完导出图片，再贴回 Obsidian——链接不能点、文字不能改，过两天思路变了，整张图又得重画。

更难受的是，你的笔记和你的图，从此活在两套世界里：笔记在 vault 里跟着双链和同步走，图却是个被「冻结」的截图。

能不能在 Obsidian 里，像打开普通笔记一样，直接打开、编辑 `.xmind` 文件？

**Mindmap Xmind** 就是为这件事而生的。

---

## ▌它是什么

Mindmap Xmind 是一款 Obsidian 第三方插件（插件 ID：`mindmap-xmind`，当前版本 `1.0.5`，MIT 开源，支持桌面端与移动端），让你在 Obsidian 内部**直接查看和交互编辑 XMind 思维导图**（`.xmind` 格式）。

不需要离开 Obsidian，不需要导出导入。文件就躺在你的 vault 里，和笔记共享同一套同步、备份、双链生态。

你得到的是一个「活」的思维导图，而不是一张死图片。

---

## ▌它现在能做什么

**◆ 像打开笔记一样打开导图**
直接点击库里的任意 `.xmind` 文件，即在 Obsidian 内以思维导图视图打开。

**◆ 真正的交互编辑**
- 选中节点后增删主题、折叠/展开分支
- 双击节点即可修改文字
- 快捷键：`Tab` 加子主题、`Enter` 加同级、`Delete` 删节点、`空格` 折叠展开、`Ctrl/Cmd+S` 保存

**◆ 一键新建**
命令面板搜索「新建思维导图」，或在文件列表右键文件夹/文件 →「新建思维导图」，文件直接落在对应目录。

**◆ 图标化工具栏 + 智能溢出**
所有操作以图标按钮呈现，悬停显示名称；窗口变窄时多余按钮自动收进「更多」下拉，始终保证至少一个按钮可用。

**◆ 缩略图（mini map）**
右下角缩略图，设置里可开关，工具栏按钮也能临时切换。

**◆ 自动保存**
编辑后按设定的间隔（10–300 秒，默认 30 秒）自动落盘，不用每次手动保存。

**◆ 丰富的外观设置**
- 默认布局（平衡、逻辑图等）
- 紧凑模式（间距更小，显示更多内容）
- 主题配色：经典 / 彩虹分支 / 柔和色板
- 显示缩略图开关、默认缩放（适应视图 / 100%）

---

## ▌幕后：我们是怎么把它磨出来的

一款好用的插件，从来不是写完第一行代码就结束了。

回看这个项目的提交记录，从第一个 commit 到现在的 `1.0.5`，我们整整迭代了 **15 次**。第一版，它只是「能打开」。但随着真实使用，问题一个接一个冒出来，每一个都被我们正面解决：

**— 工具栏太挤？**
做了图标化，按钮在窗口变窄时自动收进「更多」下拉，保证永远点得到。

**— 节点大小不分层次？**
让中心主题最大、分支居中、细节最小，层级一眼可辨。

**— 自动保存「抽风」？**
用户关了开关还在存、间隔调到最小像「随时存」、别的值又像「不存」。我们把逻辑彻底重写为「停止编辑 N 秒后按间隔防抖保存」，行为终于一致、可控，关掉开关就真的不写盘。

**— 颜色按钮「消失」了？**
根因是选中节点时被强制跳到了另一个标签页。我们改掉这个强制跳转，颜色面板回到它该在的地方。

**— 柔和色板不生效？**
因为主题信息没持久化、新建文件时来不及写颜色。我们补全了 `theme` 字段的存取和子节点的自动染色。

但最磨人的，是上线前的那道「关卡」——**Obsidian 社区市场审核**。

为了通过审核，我们连续修了多轮：移动端 `process` 崩溃、manifest 描述结尾的句号、README 许可证声明、设置标题的写法、DOM 创建方式……直到最后 `1.0.5`，连工具栏图标在部分环境下不显示这种细节（`setSvg` 改用 `createContextualFragment`）都补上了。

15 次提交，没有一次是「为了发版而发版」。每一次，都是用户反馈推着我们往前走。

这大概就是做一款好工具最真实的样子：不是一次灵感爆发，而是无数次「这里还能更好」的较真。

---

## ▌怎么装

**方式一：手动安装（推荐普通用户）**

1. 下载本仓库，将以下三个文件放进你的 vault 插件目录 `你的仓库/.obsidian/plugins/mindmap-xmind/`：
   - `main.js`
   - `manifest.json`
   - `styles.css`
2. 在 Obsidian 打开 **设置 → 第三方插件**，关闭「安全模式」，启用 **Mindmap Xmind**。

**方式二：从源码构建（开发者）**

```bash
npm install                      # 安装依赖
node esbuild.config.mjs          # 开发模式（带 watch）
node esbuild.config.mjs production   # 生产构建
```

构建产物 `main.js` 会生成在插件根目录。

---

## ▌适合谁

- 用 Obsidian 做知识管理，又离不开思维导图的人
- 受够了在笔记工具和思维导图工具之间反复横跳的人
- 希望导图能和笔记一起同步、备份、版本管理的人
- 喜欢折腾开源插件、想参与共建的开发者

---

## ▌写在最后

工具的价值，不在于它多炫，而在于它让你**少做一个动作**。

Mindmap Xmind 想做的，就是把「切到另一个 App 画个图」这个动作，彻底从你的工作流里删掉。

它开源（MIT）、免费、持续迭代。如果你也觉得「Obsidian 里就该能直接画 XMind」，不妨装上试试。

遇到问题、有好点子，欢迎到仓库提 Issue 和 PR——下一个版本的功能，可能就来自你的反馈。

---

*Mindmap Xmind · 在 Obsidian 里，让思维导图活起来。*
