# 找回节点颜色设置按钮

## 问题现象
用户在「画布」侧栏找不到设置/取消节点颜色的按钮（预设色板、自定义颜色、清除颜色等）。

## 根因
节点颜色相关控件一直放在 `renderFormatPanel()`（即「画布」标签页）的「节点样式」区域里，但只有在选中节点时才会渲染。

更关键的是，`select()` 方法在选中节点后会强制调用 `this.switchTab("markers")`，把用户自动切到「标记」标签页。于是用户点击节点后立刻离开「画布」页，导致颜色按钮被隐藏，产生「按钮不见了」的错觉。

## 修复
`src/MindMapView.ts` 中的 `select(id)` 方法：
- 移除 `this.switchTab("markers")` 的强制跳转。
- 改为调用 `this.renderSidePanel()`，仅刷新当前侧栏内容。

这样：
- 在「画布」标签页选中节点时，会直接在当前页显示「节点样式」区域和颜色按钮。
- 在「标记」/「贴纸」标签页选中节点时，只刷新对应标签页，不会跳来跳去。

## 验证与部署
- `npm run build` 通过（tsc + esbuild）。
- 新生成的 `main.js` 已复制到运行中的 Obsidian 插件目录：
  `E:/workspace/obsidian/obsidian_vault/.obsidian/plugins/mindmap-xmind/main.js`

## 使用方式
1. 点击选中任意节点（节点会出现蓝色描边高亮）。
2. 保持或切换回「画布」标签页。
3. 「节点样式」区域会显示：
   - 预设色板（点击直接设置节点颜色）
   - 自定义颜色取色器
   - 清除此节点颜色
   - 按分支自动染色
   - 清除所有节点颜色
