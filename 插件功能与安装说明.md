# Mindmap Xmind 插件功能与安装说明

Mindmap Xmind 是一款 Obsidian 第三方插件（插件 ID：`mindmap-xmind`，当前版本 `1.0.5`，MIT 开源，支持桌面端与移动端），用于在 Obsidian 内部查看和交互编辑 XMind 思维导图（`.xmind` 格式）。

本插件由个人使用 AI 辅助开发，初衷是现有的 Obsidian 思维导图类插件未能满足实际使用需求。以下仅说明其功能与安装方式。

---

## 一、功能介绍

### 1. 打开 .xmind 文件
直接点击库中的任意 `.xmind` 文件，即在 Obsidian 内以思维导图视图打开，无需导出或切换工具。

### 2. 交互编辑
- 选中节点后，可添加/删除主题、折叠/展开分支
- 双击节点即可修改文字内容

### 3. 新建思维导图
- 命令面板搜索「新建思维导图」，输入文件名创建
- 文件列表右键文件夹或文件 →「新建思维导图」，文件落在对应目录

### 4. 图标化工具栏 + 智能溢出
所有操作以图标按钮呈现，悬停显示功能名称；窗口宽度不足时，末尾按钮自动收进「更多」下拉，至少保留一个按钮可见。

### 5. 缩略图（mini map）
右下角缩略图，可在设置中开关，工具栏按钮也可临时切换。

### 6. 自动保存
编辑内容按设定间隔自动落盘，无需手动保存。

### 编辑快捷键

| 操作 | 快捷键 |
| --- | --- |
| 手动保存 | `Ctrl/Cmd + S` |
| 添加子主题 | `Tab` |
| 添加同级主题 | `Enter` |
| 删除节点 | `Delete` |
| 折叠 / 展开分支 | `空格` |
| 编辑节点文字 | 双击节点 |

### 工具栏按钮
- 适应视图、保存
- 添加子主题、添加同级、删除、折叠/展开
- 更多（导出 SVG / 导出 PNG / 缩略图 / 打开面板）

---

## 二、设置项

在 Obsidian **设置 → Mindmap Xmind** 中可配置：

| 设置项 | 说明 |
| --- | --- |
| 自动保存 | 编辑时按间隔自动保存 |
| 自动保存间隔 | 时间间隔（秒），范围 10–300，默认 30 |
| 默认布局 | 新建导图时使用的默认布局 |
| 紧凑模式 | 新建导图默认使用紧凑布局（间距更小）；已有文件以自身设置为准 |
| 主题配色 | 新建导图分支配色：经典 / 彩虹分支 / 柔和色板；已有文件以自身设置为准 |
| 显示缩略图 | 打开导图时是否默认显示右下角缩略图（默认关闭，工具栏按钮可临时切换） |
| 默认缩放 | 打开导图时的初始缩放：适应视图（自动）或 100%（原始大小） |

---

## 三、安装方式

### 方式一：手动安装（普通用户）

1. 下载本仓库，将以下三个文件放到你的 vault 插件目录 `你的仓库/.obsidian/plugins/mindmap-xmind/` 下：
   - `main.js`
   - `manifest.json`
   - `styles.css`
2. 在 Obsidian 中打开 **设置 → 第三方插件**，关闭「安全模式」，启用 **Mindmap Xmind**。

### 方式二：从源码构建（开发者）

```bash
npm install                        # 安装依赖
node esbuild.config.mjs            # 开发模式（带 watch，文件变更自动重新打包）
node esbuild.config.mjs production # 生产构建
```

构建产物 `main.js` 会生成在插件根目录。

---

## 四、项目结构

```
mindmap-xmind/
├── main.js            # 构建产物（ESM 打包）
├── manifest.json      # 插件元信息
├── styles.css         # 样式
├── esbuild.config.mjs # 构建脚本
├── src/
│   ├── main.ts        # 插件入口：命令、右键菜单、设置面板
│   ├── MindMapView.ts # 思维导图视图：工具栏、画布、交互
│   ├── model.ts       # 节点 / 导图数据模型
│   ├── xmind.ts       # .xmind 文件读写解析
│   ├── markers.ts     # 标记（图标）支持
│   └── util.ts        # 工具函数
└── sample-*.xmind     # 示例导图（功能/鱼骨图/组织架构/标记 等）
```

---

## 五、许可证

本插件以 **MIT 许可证** 发布。第三方依赖 **JSZip**（MIT 许可证）已随插件一并打包分发，遵守其原始许可证。
